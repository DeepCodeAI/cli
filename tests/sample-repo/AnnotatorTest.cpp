#include <fstream>

int main() {
    ReffedClientGraph* to_unref = nullptr;
    if (to_unref != nullptr) {}
    
  string fname = "fff";
  std::fstream f;
  f.open(fname.c_str(), std::fstream::out);
      f << "WhiteList:\n";
}
